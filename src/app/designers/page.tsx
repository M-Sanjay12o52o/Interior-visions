import DesignersPage from '@/components/Designers'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <DesignersPage />
    </div>
}

export default page