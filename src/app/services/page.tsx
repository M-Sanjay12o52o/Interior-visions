import ServicesPage from '@/components/ServicesPage'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <ServicesPage />
    </div>
}

export default page