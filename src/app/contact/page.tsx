import { FC } from 'react'
import ContactPage from '../../components/Contact'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <ContactPage />
    </div>
}

export default page