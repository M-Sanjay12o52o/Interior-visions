import AboutPage from '@/components/About'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <AboutPage />
    </div>
}

export default page